name: Check Secrets

on: [push]

jobs:
  check-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Check Secrets
        run: |
          echo ${{ secrets.REACT_APP_SUPABASE_CLIENT_URL}}
          echo ${{ secrets.REACT_APP_SUPABASE_PROJECT_KEY}}
